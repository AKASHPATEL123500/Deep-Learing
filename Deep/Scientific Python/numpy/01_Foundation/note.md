# 01 Foundataion

# 1 _Array creation ndArray_

1. `array kise crate karte hai` ---- `0D matrixs array`
2. `matrixs create karna hai` ---- `1D matrixs array`
3. `sclear , vector, matrixses` ---- `2D matrixs array [[ ]]`

# 2. Array matrixes type

1. `zeros() matrixs` ----> `all elemnts are zero in zero matrixs`

2. `ones() matrixs` ---- `all elemnst are 1 in ones matrixs and it take two argument `row` `col`ones((3,3))`

3. `full() matrixs , vector, matrixses` ---- `ek hi number ke matrixs bana deta hai mtlb matrixs me ek hi num dal deta hai isko khuch aise use karte hai full((3,3),7)`

4. `eye()` ----> `diagonal me 1 baki sab zero 0 ho jate hai `

5. `arange()` ----> `syntax : arange(start,stop,step) `

6. `linspace()` ----> `syntax : linspace(start,stop,num)`

_ex :_:

`linspace(0,100,5)`

`output` : `[0,25,50,75,100]`

`mean :  yaha automatics number ko genrate karta hai`
